package com.javaweb.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CloudinaryConfig {
    // TODO: Cấu hình Cloudinary thực tế (API key, secret, cloud name)
    // Remove duplicate bean definition to fix startup error
}
